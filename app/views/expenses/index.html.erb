<section class="hero is-info is-bold" style="min-height: 100vh;"> <!-- `is-fullwidth` would break mobile ;[ -->
  <div class="hero-head">
    <%= render 'shared/nav' %>
  </div>

  <div class="hero-body">
    <%= render 'current', account: @account %>
  </div>

  <footer class="hero-foot">
    <%= render 'shared/footer' %>
  </footer>
</section>
